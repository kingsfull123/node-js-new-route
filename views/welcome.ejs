<h1><%= title %> <%= user%></h1>

<h3>List of articles</h3>
<% if(article.length <= 0){ %>
    <p>There is no article saved</p>
<% } else { %>
    <% for(var i=0; i<article.length; i++) { %>
        <li><%= article[i].text %></li>
    <% } %>    
<% } %>        

<a href="/new">New</a>
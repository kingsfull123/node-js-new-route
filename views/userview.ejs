<h1><%= title %></h1>

<h3>List of article</h3>
<% if(article.length === 0) { %>
    <p>There is no article to display </p>
<% } else { %>    
    <% for(var item in article) { %>
        <li><%= item %></li>
    <% } %>   
<% } %>    